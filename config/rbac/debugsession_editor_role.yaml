# This rule is not used by the project kubedebugsess itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the ajou.oxan0n.me.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kubedebugsess
    app.kubernetes.io/managed-by: kustomize
  name: debugsession-editor-role
rules:
- apiGroups:
  - ajou.oxan0n.me
  resources:
  - debugsessions
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ajou.oxan0n.me
  resources:
  - debugsessions/status
  verbs:
  - get
